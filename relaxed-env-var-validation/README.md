# Relaxed Environment Variable Validation

## Overview

Allows special characters in env var names (excluding `=`).

## Use Case

.NET developers can now use their standard ENV keys without patching.

## Manifests

- `env-var-pod.yaml`
