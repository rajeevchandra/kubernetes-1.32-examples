# Dynamic Resource Allocation (DRA) Enhancements

## Overview

Dynamic Resource Allocation allows for the dynamic provisioning of specialized hardware resources, such as GPUs.

## Use Case

A financial institution runs machine learning models requiring GPUs. DRA ensures these workloads are scheduled on the right nodes.

## Manifests

- `resource-claim-template.yaml`
- `gpu-pod.yaml`
